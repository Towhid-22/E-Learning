import React from "react";
export const metadata = {
  title: "Member Ship",
  description: "Member Ship Page",
};
const layout = ({ children }) => {
  return <div>{children}</div>;
};

export default layout;
